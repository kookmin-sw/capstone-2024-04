{
  "dependencies": {
    "react-toastify": "^10.0.5"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.2.2"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx}": [
      "eslint --cache --fix",
      "prettier --cache --write"
    ]
  }
}
